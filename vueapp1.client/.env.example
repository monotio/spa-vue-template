# Copy to .env (committed defaults) or .env.local (gitignored, per-machine).
# Every VITE_ variable must also be declared in env.d.ts (strictImportMetaEnv) —
# an undeclared variable is a type error at the import.meta.env access site.
# Full workflow: docs/CONFIG.md
VITE_APP_TITLE=VueApp1
